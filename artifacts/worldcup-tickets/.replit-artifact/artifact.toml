kind = "web"
previewPath = "/"
title = "World Cup Tickets"
version = "1.0.0"
id = "artifacts/worldcup-tickets"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25170

[services.development]
run = "pnpm --filter @workspace/worldcup-tickets run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/worldcup-tickets", "run", "build" ]
publicDir = "artifacts/worldcup-tickets/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25170"
BASE_PATH = "/"
